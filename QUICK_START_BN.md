# MFFMv14 কুইক স্টার্ট ও ব্যবহার নির্দেশিকা (বাংলা সংস্করণ)

### MFFMv14 কী?

**MFFMv14 (Magisk Flashable Font Module v14)** হলো **Magisk**, **KernelSU**, এবং **APatch**-এর জন্য অ্যান্ড্রয়েড ফন্ট মডিউল তৈরির একটি স্বয়ংক্রিয় টুল এবং টেমপ্লেট। এটি অ্যান্ড্রয়েডের ডিফল্ট সিস্টেম ফন্ট (যেমন Roboto)-কে কাস্টম স্ট্যাটিক বা ভ্যারিয়েবল ফন্ট দিয়ে প্রতিস্থাপন করে এবং পূর্ণাঙ্গ সিস্টেম সামঞ্জস্য বজায় রাখে।

---

### প্রধান বৈশিষ্ট্য ও সুবিধাসমূহ

- **স্বয়ংক্রিয় ফন্ট মোড ও ফরম্যাট সাপোর্ট**: স্ট্যাটিক (`.ttf`, `.otf`) এবং ভ্যারিয়েবল (`fvar` axes) ফন্ট সাপোর্ট করে, এমনকি কম্প্রেসড ওয়েবফন্ট ফরম্যাট (`.woff`, `.woff2`) ফন্টও স্বয়ংক্রিয়ভাবে প্রসেস করতে পারে।
- **ওপেনটাইপ ফিচার ফ্রিজার (OpenType Feature Freezer)**: ফন্টের বিশেষ স্টাইল বা বিকল্প ক্যারেক্টার (যেমন open digits `ss01`, alternate letters `cv01`) সরাসরি ফন্টের মধ্যে স্থায়ীভাবে ফ্রিজ/সংযুক্ত করতে দেয়।
- **ডাইনামিক মডিউল নামকরণ**: ফন্টে সংযুক্ত করা ফিচার ট্যাগগুলো (যেমন `(ss02, cv11)`) স্বয়ংক্রিয়ভাবে `module.prop`-এর মডিউল নেম এবং তৈরি হওয়া `.zip` ফাইলের নামের সাথে যুক্ত করে দেয়।
- **স্বয়ংক্রিয় ডিজিটাল সাইনিং (Auto-Signing)**: তৈরি করা ZIP ফাইলগুলো স্বয়ংক্রিয়ভাবে ডিজিটালভাবে সাইন করে দেয়, যা ফ্ল্যাশ করার জন্য সাথে সাথেই প্রস্তুত হয়ে যায়।
- **ব্যাচ মাইগ্রেশন টুল**: পুরনো MFFM ফন্ট মডিউলগুলোকে সহজেই নতুন MFFMv14 ইঞ্জিন কোরে আপগ্রেড করে।

---

### পূর্বশর্ত ও ডিপেন্ডেন্সি ইনস্টলেশন

স্ক্রিপ্টগুলো চালানোর আগে নিশ্চিত করুন যে আপনার সিস্টেমে Python 3.9+ ইনস্টল করা আছে। এরপর টার্মিনালে নিচের কমান্ডটি চালিয়ে প্রয়োজনীয় সকল ডিপেন্ডেন্সি ইনস্টল করে নিন:

```bash
# requirements.txt থেকে প্রয়োজনীয় Python লাইব্রেরিগুলো ইনস্টল করুন
pip install -r requirements.txt
```

*(ঐচ্ছিক)* আপনি চাইলে `pipx`-এর মাধ্যমে বিশ্বব্যাপী OpenType Feature Freezer ইনস্টল করতে পারেন:
```bash
pipx install opentype-feature-freezer
```

---

### মৌলিক ব্যবহার নির্দেশিকা

- **ধাপ ১: ডিপেন্ডেন্সি ইনস্টল করুন**
  - টার্মিনালে `pip install -r requirements.txt` কমান্ডটি চালান।

- **ধাপ ২: ফন্ট ফাইল যুক্ত করুন**
  - আপনার সোর্স ফন্ট ফাইলগুলো (`.ttf`, `.otf`, `.ttc`, `.otc`, `.woff`, `.woff2`) `Fonts/` ফোল্ডারের ভেতর রাখুন।

- **ধাপ ৩: মডিউল তৈরি করুন (ইন্টারেক্টিভ পদ্ধতি)**
  - টার্মিনালে নিচের কমান্ডটি চালান:
    ```bash
    python build.py
    ```
  - স্ক্রিনে কোনো Stylistic Sets বা Character Variants সিলেক্ট করতে চান কিনা জানতে চাইলে `y` চাপুন।

- **ধাপ ৪: কমান্ড লাইনের মাধ্যমে নির্দিষ্ট ফিচার ফ্রিজ করুন (ঐচ্ছিক)**
  - সরাসরি কমান্ড লাইন থেকে নির্দিষ্ট ফিচার যুক্ত করতে:
    ```bash
    python build.py --features "ss02,ss03,cv11"
    ```

- **ধাপ ৫: পুরনো মডিউল আপগ্রেড করুন (ঐচ্ছিক)**
  - আপনার পুরনো MFFM ZIP ফাইলগুলো `Old Modules/` ফোল্ডারে রাখুন এবং চালান:
    ```bash
    python update.py
    ```

- **ধাপ ৬: ফ্ল্যাশ করুন**
  - `dist/` ফোল্ডার থেকে তৈরি হওয়া চূড়ান্ত ফ্ল্যাশেবল `.zip` ফাইলটি সংগ্রহ করুন এবং Magisk, KernelSU, বা APatch দিয়ে ইনস্টল/ফ্ল্যাশ করুন।
